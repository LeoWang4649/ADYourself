<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adyourself_fb">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!--
    Allows Glide to monitor connectivity status and restart failed requests if users go from a
    a disconnected to a connected network state.
    -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoveEboard" />
        <activity android:name=".QRCode" />
        <activity android:name=".Notification" />
        <activity android:name=".AccountDetailModify" />
        <activity android:name=".AccountDetail" />
        <activity android:name=".UserOrderHistory" />
        <activity android:name=".AdminorderDetail" />
        <activity android:name=".AdminSignin" />
        <activity android:name=".AdminMainPage" />
        <activity android:name=".MainPage" />
        <activity android:name=".SignUp" />
        <activity android:name=".UserOrderHistoryDetail" />
        <activity android:name=".QueryEboard" />
        <activity android:name=".StarteBoardQuery" />
        <activity android:name=".ReservationEboard" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>